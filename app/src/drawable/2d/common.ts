/** the setting of auxiliary line of bezier curve */
export const DASH_LINE = [15, 5]

/**
 * Shared highlight color for delete affordances — the delete-segment preview
 * overlay and the multi-select-delete line highlight. Deliberately distinct
 * from every category color in CATEGORY_NAME_COLOR_MAP.
 */
export const DELETE_HIGHLIGHT_COLOR = "rgba(255, 0, 200, 0.95)"

/** the min size of polygon */
export const MIN_SIZE = 1

/** the opacity of filling color in polygon */
export const OPACITY = [0.3]
